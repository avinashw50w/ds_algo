/*

A matrix is said to be idempotent matrix if matrix multiplied by itself return the
same matrix. The matrix M is said to be idempotent matrix if and only if M * M = M.
In idempotent matrix M is a square matrix.

So simpy multiply the two matrices and check if it's equal to the original one
 */